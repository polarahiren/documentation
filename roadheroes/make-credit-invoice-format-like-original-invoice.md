##Make Credit Invoice pdf format like Original Invoice.

- Just regenerate invoice pdf using below command, so it will create update format with new format.

```
CustomScripts::regeneratePdf(transactionID)
```